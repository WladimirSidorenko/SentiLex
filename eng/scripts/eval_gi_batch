#!/bin/sh

for f in $@; do
    echo $f >&2
    if `echo "$f" | egrep -q '/[^.]+.word2vec.'`; then
	opts='-e ISO-8859-1'
    else
	opts=''
    fi
    $(dirname $0)/eval_gi $opts data/GeneralInquirer/inqdict.txt $f >&2
done
